carre (t) = t * t;
cube (y) = y * carre (y);
nand (p, q) = Non (Et (p, q));
pi2 = carre (Pi);

fact (n) =
	Si (
		InfEgale (n, 0),
		1,
		n * fact (n - 1)
		);

? cube ( pi2 + Sin (E) );
? nand (Vrai, Faux);
? fact (6);
